---
// src/components/PostsDeferred.astro
import { getPosts } from "../pages/fakePost";

const posts = await getPosts();
---

<div>
  <h1>Ejemplo de Isla de Servidor</h1>
  <ul>
    {posts.map(post => (
      <li>
        <strong>{post.title}</strong>
        <p>{post.content}</p>
      </li>
    ))}
  </ul>
</div>
